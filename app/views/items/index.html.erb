<h1>All Items</h1>

<ul id="items">
  <% @items.each do |item| %>
    <li>
      <%= link_to item.name, item_path(item) %>
      <%= item.price %>
      <%= item.qty %>
      <%= item.url %>
    </li>
  <% end %>
</ul>
<%= link_to "Create a New Item", new_item_path %>